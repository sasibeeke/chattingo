# Chattingo Backend Environment Configuration
# Copy this file to .env and update the values

# JWT Configuration (REQUIRED)
# Generate a secure secret: openssl rand -base64 32
JWT_SECRET=1SvlTd2i8dVCYEkZ/tvW4XXFcIhY+lIvGZ0sd8oyCoI=

# Database Configuration
SPRING_DATASOURCE_URL=jdbc:mysql://db:3306/chattingo_db?createDatabaseIfNotExist=true
SPRING_DATASOURCE_USERNAME=chattingo
SPRING_DATASOURCE_PASSWORD=password123

# CORS Configuration (REQUIRED for frontend communication)
CORS_ALLOWED_ORIGINS=http://frontend:3000,http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS,PATCH
CORS_ALLOWED_HEADERS=*

# Application Configuration
SPRING_PROFILES_ACTIVE=development
SERVER_PORT=8080

# WebSocket Configuration
WEBSOCKET_ENDPOINT=/ws

# Production Database (for Docker/VPS deployment)
MYSQL_ROOT_PASSWORD=Admin@123
MYSQL_DATABASE=chattingo_db
